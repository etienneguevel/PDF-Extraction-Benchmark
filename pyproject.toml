[project]
name = "pdfextraction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ollama>=0.4.7",
    "pydantic>=2.10.6",
    "pymupdf4llm>=0.0.17",
]

[project.scripts]
hello = "pdfextraction:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
